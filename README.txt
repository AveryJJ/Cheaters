COMPILATION:
	-files: Driver.cpp, Directory.cpp, Directory.h, file.cpp, file.h, hash.cpp, hash.h
TO COMPILE:
Unzip the file, and navigate to it in your terminal. Then simply type make into the command line.

